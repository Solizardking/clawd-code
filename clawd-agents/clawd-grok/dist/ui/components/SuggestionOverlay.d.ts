import type { Theme } from "../theme.js";
export declare function SuggestionOverlay({ t, suggestions, selectedIndex, }: {
    t: Theme;
    suggestions: string[];
    selectedIndex: number;
}): import("react").ReactNode;
